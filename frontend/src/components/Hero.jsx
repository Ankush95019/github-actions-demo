const Hero = () => {
  return (
    <section id="home" className="hero">
      <h1>Welcome to GitHub Actions Demo</h1>
      <p>Build, test, and deploy with confidence using automation.</p>
      <a href="#services" className="button">
        Explore
      </a>
    </section>
  );
};

export default Hero;
